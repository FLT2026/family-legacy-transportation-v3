const assert=require('node:assert');const fs=require('node:fs');const vm=require('node:vm');
const elements=new Map(),element=(id='')=>({id,innerHTML:'',textContent:'',disabled:false,parentElement:null,addEventListener(){},appendChild(child){child.parentElement=this;elements.set(child.id,child)},insertBefore(child){child.parentElement=this;elements.set(child.id,child)});const finance=element('finance'),close=element('v37-financial-close'),addExpense=element('add-expense');close.parentElement=finance;elements.set(close.id,close);elements.set(addExpense.id,addExpense);const guards={};const document={createElement:()=>element(),getElementById:id=>elements.get(id)||null,querySelector:selector=>selector==='#finance'?finance:null,querySelectorAll:()=>[],addEventListener(type,fn,capture){if(capture)guards[type]=fn}};
const load={id:'FLT-JRN-1',invoice:{number:'INV-1',status:'Finalized',finalizedAt:'2026-09-08T00:00:00Z',invoiceDate:'2026-09-08',total:1100,lineItems:[{id:'BASE',type:'transportation',amount:1000},{id:'ACC',type:'accessorial',amount:125},{id:'CR',type:'credit',amount:-25}]},payments:[{id:'PAY-1',type:'Payment',date:'2026-09-08',method:'ACH',amount:400,allocation:{invoiceNumber:'INV-1',amount:400}}],expenses:[{category:'Fuel',vendor:'Fuel Stop',date:'2026-09-08',amount:100,receipt:{filename:'fuel.pdf',mimeType:'application/pdf',timestamp:'2026-09-08T10:00:00Z'}}],financialAdjustments:[],journalEntries:[]};let toastText='';const sandbox={document,window:{FLTExpense:{open:()=>true}},current:()=>load,store:{loads:[load]},money:value=>'$'+value.toFixed(2),persist(){},toast:value=>{toastText=value},confirm:()=>true,Date};vm.createContext(sandbox);vm.runInContext(fs.readFileSync('v37-payments-receivables.js','utf8'),sandbox);vm.runInContext(fs.readFileSync('v37-double-entry-ledger.js','utf8'),sandbox);const api=sandbox.window.FLTDoubleEntryLedger;
assert.ok(load.expenses[0].id);assert.equal(load.journalEntries.length,3);assert.equal(api.audit(load).pass,true);const count=load.journalEntries.length;assert.equal(api.sync(load).length,0);let result=api.postAdjustment(load,{type:'Credit',amount:50,date:'2026-09-09',reason:'Service credit'},{id:'CR-1',confirm:()=>true,now:new Date('2026-09-09T12:00:00Z')});assert.equal(result.ok,true);assert.equal(result.adjustment.amount,-50);assert.equal(load.journalEntries.length,4);assert.equal(api.audit(load).pass,true);assert.equal(sandbox.window.FLTPaymentsReceivables.summary(load).receivable,65000);assert.equal(api.postAdjustment(load,{type:'Adjustment',amount:10,date:'2026-09-09',reason:'Cancel'},{confirm:()=>false}).reason,'Adjustment cancelled.');
load.financialClose={status:'Closed',closedAt:'2026-09-10T00:00:00Z',snapshot:{}};const beforeAdjustments=load.financialAdjustments.length,beforeEntries=load.journalEntries.length,beforeExpenses=JSON.stringify(load.expenses);result=api.postAdjustment(load,{type:'Credit',amount:10,date:'2026-09-10',reason:'Late credit'},{confirm:()=>true});assert.equal(result.ok,false);assert.match(result.reason,/financially closed/);assert.equal(api.expenseWriteAllowed(load),false);assert.equal(api.receiptWriteAllowed(load),false);api.protectExpenseEntry();api.protectReceiptEntry();assert.equal(addExpense.disabled,true);assert.equal(sandbox.window.FLTExpense.open(),false);assert.match(toastText,/financially closed/);
let prevented=false,stopped=false;const expenseForm={querySelector:selector=>['[name="category"]','[name="vendor"]','[name="amount"]'].includes(selector)?{}:null};guards.submit({target:expenseForm,preventDefault(){prevented=true},stopImmediatePropagation(){stopped=true}});assert.equal(prevented,true);assert.equal(stopped,true);
prevented=false;stopped=false;const removeButton={closest:selector=>selector.includes('data-remove-receipt')?{}:null};guards.click({target:removeButton,preventDefault(){prevented=true},stopImmediatePropagation(){stopped=true}});assert.equal(prevented,true);assert.equal(stopped,true);assert.match(toastText,/protected correction/);assert.equal(JSON.stringify(load.expenses),beforeExpenses);
prevented=false;stopped=false;const picker={type:'file',accept:'image/*,application/pdf',value:'selected'};guards.change({target:picker,preventDefault(){prevented=true},stopImmediatePropagation(){stopped=true}});assert.equal(prevented,true);assert.equal(stopped,true);assert.equal(picker.value,'');assert.equal(JSON.stringify(load.expenses),beforeExpenses);assert.equal(load.financialAdjustments.length,beforeAdjustments);assert.equal(load.journalEntries.length,beforeEntries);
load.financialClose={status:'Correction Open',reason:'Attach missing receipt'};assert.equal(api.receiptWriteAllowed(load),true);prevented=false;guards.click({target:removeButton,preventDefault(){prevented=true},stopImmediatePropagation(){}});assert.equal(prevented,false);delete load.financialClose;
load.payments[0].amount=450;assert.equal(api.audit(load).sourceDrift,false);assert.equal(api.sync(load).length,0);load.payments[0].amount=400;load.journalEntries[0].lines[0].debit+=1;assert.equal(api.audit(load).sourceDrift,false);assert.match(elements.get('v37-double-entry-ledger').innerHTML,/Immutable journal by Load ID/);console.log('V3.7 immutable posting, closed expense and receipt locks, correction-open receipt access, balanced accounts, deduplication, source drift, and adjustment checks passed.');
